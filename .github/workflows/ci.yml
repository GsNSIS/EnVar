name: Build NSIS Plugin Dll's and Artifacts
on:
  pull_request:
    types: [opened, reopened, synchronize]
    paths:
    - '*.sln'
    - '*.vcproj'
    - '*.vcxproj'
    - 'Src/**.c'
    - 'Src/**.h'
    - 'Src/**.lib'
  push:
    paths:
    - '*.sln'
    - '*.vcproj'
    - '*.vcxproj'
    - 'Src/**.c'
    - 'Src/**.h'
    - 'Src/**.lib'
jobs:
  build:
    name: Build Dll's
    runs-on: windows-latest
    steps:
      # checkout code from repo
      - name: Checkout
        uses: actions/checkout@v1
      # use msbuild to build the solution
      - name: Build Plugin
        run: Bin\Build New-Build
        shell: cmd
      # upload artifacts
      - name: Publish Lib Artifact
        uses: actions/upload-artifact@v1
        with:
          name: Lib-${{ github.sha }}
          path: Lib
      - name: Publish Obj Artifact
        uses: actions/upload-artifact@v1
        with:
          name: Obj-${{ github.sha }}
          path: Obj
      # Prepare distribution folder
      - name: Create Distribution
        run: Bin\Build New-Distribution, Compress-Distribution
      # Publish Distribution Artifact
      - name: Publish Distribution Artifact
        uses: actions/upload-artifact@v1
        with:
          name: EnVar-Plugin-${{ github.sha }}
          path: Data
